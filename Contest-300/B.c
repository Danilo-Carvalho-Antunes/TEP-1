#include<stdio.h>

int main(){
    int H, W;

    scanf("%d %d",&H,&W);

    char A[H][W];
    char B[H][W];
    char c;

    for(int i = 0 ; i < H ; i++){

        for(int j = 0 ; j < W ; j++){
            char carcter;
            scanf(" %c",&A[i][j]);
        }
    }

    for(int i = 0 ; i < H ; i++){

        for(int j = 0 ; j < W ; j++){
            char carcter;
            scanf(" %c",B[i][j]);
        }
    }

    while(A[H][W] != B[H][W]){
        for(; ;){
            
        }
    }







}